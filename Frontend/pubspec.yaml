name: fyp_frontend
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
